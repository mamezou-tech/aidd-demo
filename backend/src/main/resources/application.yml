spring:
  datasource:
    url: jdbc:mysql://talent-mysql:3306/talent_management
    username: talent_user
    password: talent_pass
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
  session:
    timeout: 30m

server:
  port: 8080
